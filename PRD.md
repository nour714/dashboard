# 📋 وثيقة متطلبات المنتج الشاملة (Product Requirements Document - PRD)
# مشروع منصة AfricaTravel — نظام إدارة وحجز عمليات الطيران والعمليات المالية والرقابة الإدارية

---

## 1. نظرة عامة ورؤية المنتج (Product Overview & Vision)

**AfricaTravel** هي منصة ويب مؤسسية متخصصة ومصممة خصيصاً لوكالات السفر وشركات حجز الطيران في الشرق الأوسط وإفريقيا (Aviation ERP & Operations Terminal). تهدف المنصة إلى أتمتة دورة حياة التذاكر الجوية بالكامل — بدءاً من القراءة الذكية للوثائق (AI Ticket Extraction) وإصدار التذاكر لرحلات الذهاب والذهاب والعودة، وإدارة ملفات العملاء وتوثيق جوازات السفر سحابياً، وصولاً إلى التسجيل الدقيق للدفعات والاسترجاعات وتعديل الرحلات ومتابعة المصروفات التشغيلية. كما توفر المنصة تحليلات تنفيذية حية (Real-time Business Intelligence) مع تطبيق صارم لعزل الصلاحيات وحماية البيانات التجارية الحساسة (RBAC & Financial Privacy).

---

## 2. أدوار المستخدمين ومصفوفة الصلاحيات (User Roles & Permissions)

تعتمد المنصة 3 أدوار رئيسية مطبقة في وسيط التوثيق (`server/src/middleware/auth.js`) وموزعة عبر جميع نقاط نهاية الـ API والواجهات الرسومية:

1. **مدير النظام (`ADMIN`):** يمتلك كامل الصلاحيات الإدارية والمالية، بما في ذلك الاطلاع على وتعديل أسعار التكلفة (`costPrice`) وأسعار البيع (`ticketPrice`)، ومعالجة الاسترجاعات (`refunds`)، وإدارة الموظفين (`employees`)، وحذف التذاكر والعملاء والمصروفات، وإعدادات النظام.
2. **وكيل الحجز (`AGENT`):** مسؤول عن العمليات اليومية: إصدار التذاكر، تسجيل الدفعات الإضافية، تسجيل تعديلات الرحلات (`modifications`)، إدارة ملفات العملاء ورفع جوازاتهم، وتسجيل المصروفات الخاصة به. **محجوب عنه تماماً سعر التكلفة وصافي الربح، وتعديل سعر بيع التذكرة، وإصدار الاسترجاعات، والحذف المالي، وإدارة الموظفين.**
3. **موظف إصدار فقط (`TICKET_ONLY`):** موظف ميداني أو مدخل بيانات ذو صلاحيات مقيدة بالحد الأدنى: يحق له فقط إصدار التذاكر الجديدة واستخراج بيانات التذاكر عبر الذكاء الاصطناعي، وعرض تفاصيل التذاكر التي قام بإصدارها بنفسه فقط (محمي بنظام منع الـ IDOR). محجوب عنه الوصول إلى لوحة التحكم، وقائمة التذاكر العامة، والعملاء، والدفعات الإضافية، والتعديلات، والاسترجاعات، والمصروفات، والتقارير، والإعدادات، والموظفين.

### مصفوفة الصلاحيات التفصيلية (RBAC Permission Matrix):

| الوظيفة / نقطة النهاية (Endpoint & Feature) | مدير النظام (`ADMIN`) | وكيل الحجز (`AGENT`) | موظف إصدار فقط (`TICKET_ONLY`) | السلوك والرمز المرجع عند الرفض |
| :--- | :---: | :---: | :---: | :--- |
| **تسجيل الدخول وإدارة الجلسة (`POST /auth/login`, `/refresh`)** | ✅ | ✅ | ✅ | معدل طلبات مقيد (10 محاولات / 15 دقيقة للـ login) |
| **تحديث الملف الشخصي وتغيير كلمة المرور (`PATCH /auth/profile`, `/change-password`)** | ✅ | ✅ | ✅ | إبطال كافة الجلسات الأخرى فور تغيير كلمة المرور |
| **إنهاء الجلسات الأخرى (`POST /auth/revoke-other-sessions`)** | ✅ | ✅ | ✅ | إبطال كافة توكنات التجديد باستثناء الجلسة الحالية |
| **استخراج بيانات التذاكر بالذكاء الاصطناعي (`POST /tickets/extract-from-document`)** | ✅ | ✅ | ✅ | متاح للجميع؛ يحجب `costPrice` آلياً ويحدد 5 طلبات تزامنية |
| **إصدار التذاكر (`POST /tickets`)** | ✅ | ✅ | ✅ | إذا أرسل غير الـ ADMIN حقل `costPrice` يتم تصفيره تلقائياً إلى 0 |
| **عرض قائمة التذاكر والبحث المتقدم (`GET /tickets`)** | ✅ | ✅ | ❌ | `403 Forbidden` لموظفي `TICKET_ONLY` |
| **عرض تفاصيل تذكرة محددة (`GET /tickets/:id`)** | ✅ (جميع التذاكر) | ✅ (جميع التذاكر) | ✅ (التذاكر الصادرة منه فقط) | `403 Forbidden` عند محاولة `TICKET_ONLY` فتح تذكرة لغيره (IDOR Guard) |
| **الاطلاع على حقول التكلفة والأرباح (`costPrice` & `netProfit`)** | ✅ | ❌ | ❌ | محذوف بالكامل من استجابة الـ API والواجهة الرسومية لغير الـ ADMIN |
| **تعديل بيانات التذكرة العامة (تصحيح الأخطاء) (`PATCH /tickets/:id`)** | ✅ | ✅ | ❌ | `403 Forbidden` لـ `TICKET_ONLY` |
| **تعديل سعر التذكرة أو التكلفة (`PATCH /tickets/:id` مع `ticketPrice`/`costPrice`)** | ✅ | ❌ | ❌ | `403 Forbidden` للوكيل؛ التحقق من `PRICE_BELOW_PAID` للأدمن |
| **تسجيل دفعة جديدة على التذكرة (`POST /tickets/:id/payments`)** | ✅ | ✅ | ❌ | `403 Forbidden` لـ `TICKET_ONLY` |
| **تسجيل تعديل رحلة ورسوم التغيير (`POST /tickets/:id/modifications`)** | ✅ | ✅ | ❌ | `403 Forbidden` لـ `TICKET_ONLY` |
| **معالجة استرجاع تذكرة (`POST /tickets/:id/refunds`)** | ✅ | ❌ | ❌ | `403 Forbidden` للوكيل وموظف الإصدار |
| **حذف التذكرة مع سجلاتها المالية (`DELETE /tickets/:id`)** | ✅ | ❌ | ❌ | `403 Forbidden` للوكيل وموظف الإصدار (تسجيل لقطة مالية بالـ Audit) |
| **الحذف النهائي للتذكرة (`DELETE /tickets/:id/purge`)** | ✅ (بشروط صارمة) | ❌ | ❌ | يتطلب أن تكون محذوفة مبدئياً وخالية من أي حركات مالية وتطابق الـ ID |
| **عرض وإدارة العملاء وملاحظاتهم (`GET/POST/PATCH /customers`, `/notes`)** | ✅ | ✅ | ❌ | `403 Forbidden` لـ `TICKET_ONLY` |
| **رفع والاطلاع على مستند جواز السفر (`POST/GET /customers/:id/passport-document`)** | ✅ | ✅ | ❌ | رابط موقع مؤقت (Signed URL) صالح لمدة 300 ثانية |
| **حذف مستند جواز السفر سحابياً (`DELETE /customers/:id/passport-document`)** | ✅ | ❌ | ❌ | `403 Forbidden` للوكيل وموظف الإصدار |
| **الحذف المبدئي والنهائي للعميل (`DELETE /customers/:id`, `/purge`)** | ✅ (بدون تذاكر) | ❌ | ❌ | `403 Forbidden` للوكيل؛ الحذف النهائي يتطلب عدم وجود تاريخ حجز |
| **إدارة الموظفين والصلاحيات بالكامل (`/employees/*`)** | ✅ | ❌ | ❌ | `403 Forbidden` للوكيل وموظف الإصدار (حظر تخفيض أو حذف النفس أو آخر أدمن) |
| **تسجيل واستعراض المصروفات المكتبية (`GET/POST /expenses`)** | ✅ (يرى الكل) | ✅ (يرى ما أنشأه فقط) | ❌ | تصفية تلقائية حسب `createdById` للوكلاء؛ `403 Forbidden` لـ `TICKET_ONLY` |
| **حذف سجل مصروف مكتبي (`DELETE /expenses/:id`)** | ✅ | ❌ | ❌ | `403 Forbidden` للوكيل وموظف الإصدار |
| **التقارير التنفيذية والمالية (`/reports/*`)** | ✅ | ✅ (مبيعات ومسارات) | ❌ | `403 Forbidden` لـ `TICKET_ONLY`؛ حجب تقارير التكلفة عن الوكيل |
| **سجل التدقيق والنشاطات (`GET /activity`)** | ✅ | ✅ | ❌ | `403 Forbidden` لـ `TICKET_ONLY` |
| **تعديل إعدادات النظام (`PATCH/PUT /settings`)** | ✅ | ❌ | ❌ | عرض الإعدادات متاح للوكيل (`GET`)، التعديل للأدمن فقط |

---

## 3. المميزات والوظائف التفصيلية (Features & Acceptance Criteria)

### 3.1. التوثيق وإدارة الجلسات والأمان (Authentication & Session Management)

- **الوصف:** بنية أمنية متكاملة تعتمد على نظام الرموز المزدوجة (Dual-Token Architecture): رمز وصول قصير الأجل في الذاكرة (`accessToken` لمدة 15 دقيقة) وتوكن تجديد مشفر في كوكي محمي (`refreshToken` لمدة 7 أيام مع دوران تلقائي وحماية ضد إعادة الاستخدام).
- **قصة المستخدم (User Story):**
  > "بصفتي مستخدم للنظام (مدير، وكيل، أو موظف تذاكر)، أريد تسجيل الدخول بأمان إلى حسابي واستمرارية الجلسة دون انقطاع أثناء العمل، مع حماية حسابي من التسلل أو السرقة وسهولة إنهاء الجلسات الأخرى."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. بالنظر إلى بيانات اعتماد صحيحة (بريد وكلمة مرور)، عندما يرسل المستخدم طلباً إلى `POST /api/auth/login`، يستجيب الخادم بحالة `200 OK` وجسم يحتوي على بيانات المستخدم (بدون `passwordHash`) وتوكن الوصول `accessToken` وكوكي `refreshToken`، ويسجل حركة `LOGIN` في سجل التدقيق.
  2. بالنظر إلى بريد إلكتروني غير موجود أو كلمة مرور خاطئة، عندما يرسل العميل طلباً إلى `POST /api/auth/login`، يرجع الخادم حالة `401 Unauthorized` مع الرمز `INVALID_CREDENTIALS` ورسالة عامة تمنع تخمين الحسابات (Anti-Account Enumeration).
  3. بالنظر إلى حساب موظف معطل (`status = 'INACTIVE'`)، عند محاولة تسجيل الدخول بكلمة مرور صحيحة، يرفض الخادم الطلب بحالة `400 Bad Request` والرمز `ACCOUNT_INACTIVE`.
  4. بالنظر إلى تجاوز 10 محاولات تسجيل دخول خاطئة من نفس عنوان الـ IP خلال 15 دقيقة، يرد الخادم بحالة `429 Too Many Requests` والرمز `RATE_LIMIT_EXCEEDED` مع ترويسة `Retry-After`.
  5. بالنظر إلى توكن تجديد صالح، عندما يطلب العميل `POST /api/auth/refresh`، يقوم الخادم بإبطال التوكن القديم ذرياً وإصدار توكن تجديد جديد وتوكن وصول جديد (Token Rotation).
  6. بالنظر إلى محاولة تقديم توكن تجديد تم إبطاله مسبقاً (Replay Attack)، يكتشف الخادم محاولة الاختراق ويرد بـ `401 Unauthorized` بالرمز `INVALID_REFRESH_TOKEN`، ويقوم فوراً بإبطال **كافة** توكنات التجديد النشطة للمستخدم لحماية حسابه.
  7. بالنظر إلى مستخدم مسجل دخوله، عندما يطلب `POST /api/auth/change-password` بكلمة المرور الحالية وكلمة مرور جديدة (لا تقل عن 8 أحرف)، يقوم الخادم بتشفير الكلمة الجديدة بـ `bcrypt` (معامل تمليح 12)، ويبطل كافة توكنات التجديد الخاصة به لفرض تسجيل الدخول مجدداً.
  8. بالنظر إلى طلب `POST /api/auth/revoke-other-sessions` من مستخدم موثق، يقوم الخادم بتعطيل جميع جلسات المستخدم الأخرى مع الإبقاء على الجلسة الحالية نشطة ويرجع عدد الجلسات الملغاة.
  9. في كل طلب موثق يمر عبر وسيط `authenticate`، يقوم الخادم بالتحقق من قاعدة البيانات في الوقت الحقيقي من دور المستخدم وحالته النشطة؛ إذا تم تخفيض رتبة المستخدم أو تعطيل حسابه، يُرفض الطلب فوراً بـ `401 Unauthorized` والرمز `ACCOUNT_INACTIVE` حتى لو كان الـ JWT ساري المفعول زمنياً.

---

### 3.2. استخراج بيانات التذاكر بالذكاء الاصطناعي (AI Document Ticket Extraction)

- **الوصف:** خدمة مؤتمتة متصلة بنموذج Google Gemini تستخرج بيانات حجز الطيران آلياً من ملفات الـ PDF والصور المرفوعة (تذاكر إلكترونية، إيصالات، قسائم صعود) لتعبئة نموذج إنشاء التذكرة مسبقاً، مع مراعاة دقيقة لمحطات الترانزيت ورحلات الذهاب والعودة والأسماء بصيغة خطوط الطيران.
- **قصة المستخدم (User Story):**
  > "بصفتي موظف حجز أو وكيل، أريد رفع صورة أو ملف PDF للتذكرة الصادرة من شركة الطيران ليقوم النظام بقراءة تفاصيل الرحلة والراكب وتعبئتها في النموذج تلقائياً، لتوفير الوقت وتفادي أخطاء الإدخال اليدوي."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. بالنظر إلى رفع ملف من أنواع (`application/pdf`, `image/jpeg`, `image/png`) لا يتجاوز 15 ميجابايت إلى `POST /api/tickets/extract-from-document`، يستجيب النظام بحالة `200 OK` مع كائن البيانات المستخرجة استشارياً (Advisory Data).
  2. بالنظر إلى ملف غير صالح أو تزييف الامتداد (ملف نصي بامتداد `.jpg`)، يرفض وسيط الفحص المبكر `EarlyMagicByteStorage` قراءة الملف ويرجع خطأ `400 Bad Request` بالرمز `INVALID_FILE_TYPE`.
  3. في حال عدم تكوين مفتاح `GEMINI_API_KEY` على الخادم، يرجع الطلب فوراً بحالة `503 Service Unavailable` والرمز `AI_EXTRACTION_UNAVAILABLE`.
  4. تخضع نقطة النهاية لمحدد تزامني صارم (`aiExtractionConcurrencyBudget`) بحد أقصى 5 عمليات استخراج متزامنة، ويرد بـ `429 Too Many Requests` والرمز `AI_EXTRACTION_CONCURRENCY_LIMIT_EXCEEDED` في حال تجاوز الميزانية.
  5. يدعم محرك الاستخراج إعادة المحاولة التلقائية على نموذج بديل: يبدأ بالنموذج الأساسي (`gemini-3.7-flash` افتراضياً)، وفي حال فشل الاتصال أو خطأ من واجهة Google، يعيد المحاولة على النموذج الاحتياطي (`gemini-3.5-flash-lite`)، ويرجع `502 Bad Gateway` بالرمز `AI_EXTRACTION_FAILED` فقط عند فشل كلاهما.
  6. **حماية التكلفة:** يتم استبعاد وتجريد أي حقل متعلق بسعر التكلفة (`costPrice`) نهائياً من مخرجات الاستخراج ولا يُقترح أبداً من الذكاء الاصطناعي.
  7. **قاعدة الترانزيت الصارمة:** عند وجود رحلة متعددة المقاطع تتضمن محطة توقف (مثل CAI → IST ثم IST → LHR)، يجب على النظام تحديد الوجهة النهائية الحقيقية (`LHR`)، ويُحظر تماماً الإبلاغ عن مطار الترانزيت الأوسط (`IST`) كوجهة للرحلة.
  8. **توحيد رقم الرحلة:** في الرحلات الترانزيت التي تشتمل على أكثر من رقم رحلة للمسار الذهابي، يتم قراءة واقتطاع رقم الرحلة الأولى فقط المغادرة من نقطة الانطلاق (First Outbound Leg e.g. `ET 0453`) وتجاهل الأرقام اللاحقة لمنع دمجها بفواصل.
  9. **قائمة المطارات المعتمدة:** يتم التحقق من رموز المطارات الثلاثية (IATA) مقابل قائمة بيضاء معتمدة (`VALID_IATA_AIRPORT_CODES`)؛ إذا كان الرمز غير مدرج بالقائمة (مثل مطار إقليمي جديد)، **لا يتم حذفه أو تفريغه**، بل يُعاد الرمز كما هو مع إدراجه في مصفوفة `unrecognizedFields: ['destination']` ليقوم الموظف بتأكيده بصرياً في الواجهة.
  10. **التعرف على نوع الرحلة:** إذا لم تتضمن الوثيقة رحلة عودة بتاريخ مستقل ومسار عكسي، يتم ضبط `tripType = 'One Way'` وتجريد كافة حقول `return*`.

---

### 3.3. إصدار التذاكر والربط التلقائي بالعميل (Ticket Creation & Customer Link)

- **الوصف:** نموذج متطور لإصدار تذاكر الطيران يدعم رحلات الذهاب (`One Way`) والذهاب والعودة (`Round Trip`)، مع إنشاء معرفات فريدة مقاومة للتصادم، وربط التذكرة تلقائياً بملف العميل عبر رقم الجواز، ودعم تسجيل دفعة أولى اختيارية.
- **قصة المستخدم (User Story):**
  > "بصفتي وكيل حجز أو موظف إصدار، أريد إصدار تذكرة طيران جديدة وإدخال بيانات الراكب والرحلة والماليات، بحيث يقوم النظام بتسجيلها وتحديث حساب العميل آلياً."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. عندما يرسل مستخدم موثق طلباً صالحاً إلى `POST /api/tickets`، ينشئ الخادم التذكرة بمعرف فريد بصيغة `TK-XXXXXXXX` ويرجع حالة `201 Created` مع البيانات وحسابات الدفاتر المالية المرفقة (`financials`).
  2. إذا تم تقديم `customerId` محدد وكان العميل غير موجود أو محذوفاً مبدئياً (`deletedAt != null`)، يرفض الطلب بـ `404 Not Found`.
  3. إذا لم يُرسل `customerId`، يبحث النظام عن عميل نشط يطابق رقم جواز السفر (`passport`) المرسل بحالة عدم تحسس للأحرف (Case-insensitive)؛ فإذا وُجد يربط التذكرة به، وإذا لم يوجد ينشئ ملف عميل جديد تلقائياً بالمعرف `CUST-XXXXXXXX`.
  4. إذا أُرسل `ticketNumber` مكرر لتذكرة نشطة سابقة، يرد الخادم بحالة `409 Conflict` والرمز `DUPLICATE_TICKET_NUMBER`.
  5. إذا أُرسل `pnr` مكرر لتذكرة نشطة، يرد الخادم بـ `409 Conflict` والرمز `DUPLICATE_PNR`.
  6. إذا قام مستخدم بدور `AGENT` أو `TICKET_ONLY` بإرسال حقل `costPrice` بقيمة ما، يتجاهل النظام القيمة ويخزن `costPrice: 0` في قاعدة البيانات ويحجب الحقل من الاستجابة.
  7. يتم تحديد نوع الرحلة آلياً: إذا تم توفير تاريخ العودة `returnDepartureDate`، تُسجل الرحلة كـ `Round Trip`، ويُشترط أن يكون تاريخ العودة لاحقاً لتاريخ الذهاب.
  8. إذا تم تمرير دفعة أولى (`initialPayment > 0`)، يتم التحقق ألا تتجاوز سعر التذكرة (`ticketPrice`)، وينشئ الخادم سجلاً في جدول المدفوعات `Payment` بالمعرف `PAY-XXXXXXXX` مع ضبط حالة التذكرة على `PARTIALLY PAID` أو `CONFIRMED` إذا دفعت بالكامل.

---

### 3.4. تعديل وتصحيح التذاكر وحماية الأسعار (Ticket Editing & Price Guards)

- **الوصف:** واجهة تصحيح وتعديل بيانات التذكرة (`PATCH /api/tickets/:id`) المخصصة لتصحيح أخطاء الإدخال (Typo Corrections) في اسم الراكب، رقم الـ PNR، رقم التذكرة، تفاصيل الرحلة، وتعديل الأسعار للمدراء، مع حظر تقليص السعر لما دون المدفوع دون تأكيد.
- **قصة المستخدم (User Story):**
  > "بصفتي وكيل حجز أو مدير، أريد تصحيح خطأ إملائي في اسم المسافر أو رقم التذكرة أو رقم الرحلة بسهولة وسرعة من نافذة التعديل لتحديث بيانات الحجز دون الإخلال بالسجلات التاريخية."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. بالنظر إلى مستخدم بدور `AGENT` أو `ADMIN`، عندما يرسل تعديلات على حقول المسافر أو المسار إلى `PATCH /api/tickets/:id`، يرجع الخادم حالة `200 OK` بالتذكرة المعدلة، ويسجل ملخصاً تفصيلياً بالحقول المتغيرة في سجل التدقيق (`UPDATE_TICKET`).
  2. بالنظر إلى محاولة من مستخدم بدور `TICKET_ONLY` لتعديل أي تذكرة، يرفض الخادم الطلب بـ `403 Forbidden`.
  3. بالنظر إلى محاولة من مستخدم بدور `AGENT` لتعديل حقل `ticketPrice` أو `costPrice`، يرفض الخادم الطلب فوراً بحالة `403 Forbidden` برسالة توضح أن تعديل الأسعار قاصر على المدراء.
  4. **حظر تقليص السعر دون تأكيد:** بالنظر إلى تذكرة دفع العميل لها مبلغاً قدره `7000 EGP`، عندما يحاول مدير تخفيض `ticketPrice` إلى `5000 EGP` دون إرسال `confirmPriceBelowPaid: true`، يرفض الخادم الطلب بحالة `409 Conflict` والرمز `PRICE_BELOW_PAID`؛ وعند إعادة الإرسال مع `confirmPriceBelowPaid: true` يوافق الخادم على التعديل.
  5. عند تعديل التذكرة لاستهداف تذكرة محذوفة مبدئياً (`deletedAt != null`)، يرجع الخادم `404 Not Found`.

---

### 3.5. تعديل مواعيد ومسارات الرحلات (Flight Schedule Modifications)

- **الوصف:** مسار العمليات التشغيلية الرسمي لتسجيل التغييرات الطارئة على حجز العميل بطلب منه أو من شركة الطيران (Flight Reschedule / Route Change)، وتوثيق رسوم التعديل (`changeFee`) في سجل العمليات.
- **قصة المستخدم (User Story):**
  > "بصفتي وكيل حجز، أريد تسجيل طلب العميل بتغيير موعد السفر مع تسجيل رسوم التعديل المقررة من شركة الطيران والسبب، لتوثيق تاريخ التغييرات وتحديث مديونية التذكرة."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. عندما يرسل وكيل أو مدير طلباً إلى `POST /api/tickets/:id/modifications` يحتوي على تاريخ الإقلاع الجديد والرسوم والسبب، ينشئ الخادم سجلاً في جدول `Modification` بالمعرف `MOD-UUID`.
  2. يقوم الخادم بالتقاط لقطة لبيانات الرحلة الأصلية (`originalFlight`) وتخزين بيانات الرحلة المعدلة (`newFlight`) ورسوم التعديل وسبب الطلب وهوية الموظف المنفذ.
  3. يقوم الخادم بتحديث تاريخ الإقلاع (`departureDate`) على سجل التذكرة الفعلي ليطابق التاريخ الجديد.
  4. تضاف رسوم التعديل آلياً إلى حساب إجمالي القيمة الصافية للتذكرة (`netValue = ticketPrice + totalModificationFees - totalRefunded`).
  5. إذا كان تاريخ الوصول الجديد أسبق من تاريخ الإقلاع الجديد، يرفض الخادم الطلب بـ `400 Bad Request` والرمز `ARRIVAL_BEFORE_DEPARTURE`.

---

### 3.6. المعاملات المالية والدفعات المجزأة (Payments & Ledger Management)

- **الوصف:** إدارة الدفاتر المالية للتذكرة وتسجيل دفعات مالية متعددة من العميل بوسائل دفع مختلفة مع عزل المعاملات ذرياً (Serializable Transactions) وحساب الرصيد المتبقي بدقة متناهية عبر مكتبة `decimal.js`.
- **قصة المستخدم (User Story):**
  > "بصفتي وكيل حجز، أريد تسجيل دفعة نقدية أو بنكية إضافية لتذكرة العميل، بحيث يتم تحديث الرصيد المتبقي وحالة الدفع للتذكرة فوراً ودون أخطاء تقريب مالي."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. عند إرسال طلب تسجيل دفعة إلى `POST /api/tickets/:id/payments` بمبلغ صالح ووسيلة دفع مقبولة، ينشئ الخادم حركة دفع `PAY-UUID` بحالة `201 Created`.
  2. **حظر الدفع الزائد:** إذا كان المبلغ المدفوع يتجاوز الرصيد المتبقي الفعلي للتذكرة (`amount > remaining`)، يرفض الخادم الطلب بحالة `400 Bad Request` والرمز `PAYMENT_EXCEEDS_BALANCE`.
  3. وسائل الدفع المدعومة والمعتمدة في التحقق تشمل: `Cash`, `Credit Card`, `Bank Transfer`, `Vodafone Cash`, `InstaPay`, `Corporate Credit`.
  4. تتحول حالة التذكرة آلياً: إلى `PARTIALLY PAID` إذا كان المدفوع أكبر من صفر وأقل من السعر، وإلى `CONFIRMED` إذا تمت تغطية كامل سعر التذكرة.
  5. تنفذ عملية الدفع داخل معاملة قاعدة بيانات متسلسلة (`Serializable Transaction`) لمنع تضارب الطلبات المتزامنة (Race Conditions).

---

### 3.7. إدارة الاسترجاعات النقدية (Refunds Management)

- **الوصف:** المعالجة المالية لطلبات الاسترجاع الكلي أو الجزئي للتذاكر، وهي عملية مقتصرة حصرياً على مدراء النظام (`ADMIN`) وتخضع لمعادلة الحد الأقصى للمبلغ القابل للاسترداد.
- **قصة المستخدم (User Story):**
  > "بصفتي مدير نظام، أريد معالجة استرجاع تذكرة ملغاة وخصم رسوم الإلغاء، مع ضمان عدم استرجاع مبالغ تتجاوز ما دفعه العميل فعلياً."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. إذا حاول مستخدم بدور `AGENT` أو `TICKET_ONLY` إرسال طلب استرجاع إلى `POST /api/tickets/:id/refunds`، يرفض الخادم فوراً بحالة `403 Forbidden`.
  2. عندما يرسل مدير النظام طلباً صالحاً للاسترجاع، ينشئ الخادم سجلاً في جدول `Refund` بالمعرف `RF-UUID`.
  3. **قيد المبلغ القابل للاسترداد:** يُحسب الرصيد المتاح للاسترجاع بالمعادلة `availableRefund = totalPaid - totalRefunded`؛ فإذا كان مبلغ الاسترجاع المطلوب أكبر من المتاح، يرفض الطلب بـ `400 Bad Request` والرمز `REFUND_EXCEEDS_AVAILABLE`.
  4. تتغير حالة التذكرة آلياً: إذا غطت الاسترجاعات كامل المبلغ المدفوع تصبح التذكرة `REFUNDED`؛ وإذا غطت جزءاً منه تصبح `PARTIALLY_REFUNDED`.

---

### 3.8. إدارة العملاء وملاحظات الـ CRM (Customer Management & CRM)

- **الوصف:** دليل العملاء، تتبع تاريخ سفر كل عميل، إجمالي إنفاقه التراكمي، رصيده المعلق، وسجل الملاحظات والتفضيلات، مع ميزة التمييز للعملاء المميزين (`VIP Badge`).
- **قصة المستخدم (User Story):**
  > "بصفتي وكيل حجز أو مدير، أريد استعراض سجل العميل وكافة تذاكره السابقة ودفعاته وتدوين ملاحظات خاصة بتفضيلاته لتحسين جودة خدمته."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. عند طلب قائمة العملاء `GET /api/customers`، يدعم النظام الترقيم والبحث بالاسم أو الهاتف أو البريد أو رقم الجواز بحالة عدم تحسس للأحرف.
  2. يمنع النظام تكرار رقم جواز السفر للعملاء النشطين؛ وإذا تم إرسال رقم جواز موجود مسبقاً لعميل نشط، يرجع `409 Conflict` والرمز `DUPLICATE_PASSPORT`.
  3. يمكن للوكيل أو المدير إضافة ملاحظة لملف العميل عبر `POST /api/customers/:id/notes` وتُسجل مع اسم كاتبها وتاريخها.
  4. يحسب ملف العميل ديناميكياً إجمالي عدد التذاكر، إجمالي المبالغ المدفوعة، والمبالغ المتبقية عبر التجميع المباشر للتذاكر النشطة غير المحذوفة.
  5. عند حذف عميل عبر `DELETE /api/customers/:id` من قِبل المدير، يتم تنفيذ حذف مبدئي بتعيين `deletedAt = now()` واستبعاده من القوائم العادية.
  6. عند طلب الحذف النهائي للعميل `DELETE /api/customers/:id/purge`، يرفض النظام بـ `400 Bad Request` والرمز `CUSTOMER_NOT_SOFT_DELETED` إذا لم يكن العميل محذوفاً مبدئياً، ويرفض بالرمز `CUSTOMER_HAS_TICKET_HISTORY` إذا كان للعميل أي سجل تذاكر تاريخي، ويتطلب تطابق تأكيد المعرف `confirmCustomerId`.

---

### 3.9. رفع وتوثيق مستندات جوازات السفر سحابياً (Passport Documents Cloud Storage)

- **الوصف:** رفع صور ووثائق جوازات سفر المسافرين وحفظها بأمان في حاوية سحابية خاصة (Private Supabase Storage Bucket: `customer-documents`)، مع إنشاء روابط استعراض مؤقتة والتنظيف الذاتي للملفات القديمة أو اليتيمة.
- **قصة المستخدم (User Story):**
  > "بصفتي وكيل حجز، أريد رفع صورة أو مستند PDF لجواز سفر العميل ليتمكن موظفو الوكالة من مراجعته وإصدار التأشيرات، مع ضمان عدم تسريب الملفات العامة أو ترك ملفات مهملة."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. يدعم رفع الملفات عبر `POST /api/customers/:id/passport-document` بامتدادات (`.jpg`, `.jpeg`, `.png`, `.pdf`) وبحد أقصى للطلب قدره 4.5 ميجابايت على منصة Vercel و15 ميجابايت على وسيط Multer.
  2. يفحص النظام البصمة السحرية للملف عبر دالتين: `EarlyMagicByteStorage` أثناء تدفق البيانات، ومكتبة `file-type` على الـ Buffer في الخدمة؛ وأي ملف غير مطابق للبصمة الحقيقية يُرفض فوراً بـ `400 Bad Request` بالرمز `VALIDATION_ERROR`.
  3. **آلية استبدال المستند بأمان (Zero-Orphan Architecture):** عند استبدال جواز سفر موجود:
     - أ) يُرفع الملف الجديد إلى السحابة بالمسار `customers/{id}/passport-{uuid}.{ext}`.
     - ب) يتم تحديث سجل العميل في قاعدة البيانات بالمسار الجديد.
     - ج) في حال فشل تحديث قاعدة البيانات، يقوم الخادم بإجراء تعويضي فوري بحذف الملف الجديد المرفوع لمنع الملفات اليتيمة.
     - د) في حال نجاح تحديث قاعدة البيانات، يقوم النظام بحذف الملف القديم من السحابة.
  4. عند طلب استعراض الجواز `GET /api/customers/:id/passport-document`، ينشئ الخادم رابطاً موقعاً ومؤقتاً (Signed URL) صالحاً لمدة 300 ثانية فقط، ويسجل حركة `PASSPORT_DOCUMENT_VIEWED` متضمنة عنوان IP والـ UserAgent للمستخدم في سجل التدقيق.
  5. حذف مستند الجواز `DELETE /api/customers/:id/passport-document` متاح لمدير النظام فقط، ويؤدي لمسح الملف من السحابة وتصفير حقلي `passportDocPath` و`passportDocUploadedAt`.

---

### 3.10. إدارة الموظفين والأمان الداخلي (Staff Management & Internal Controls)

- **الوصف:** إدارة حسابات الموظفين وصلاحياتهم ومتابعة أدائهم التشغيلي، ومحمية بضوابط أمنية ضد تخمين الحسابات وعزل المدراء.
- **قصة المستخدم (User Story):**
  > "بصفتي مدير النظام العام، أريد إنشاء حسابات لموظفي الحجز وتحديد أدوارهم ومتابعة مبيعاتهم، مع التأكد من عدم قدرة أي موظف على التلاعب بالصلاحيات أو قفل النظام على نفسه."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. كافة نقاط نهاية `/api/employees/*` محصورة بمدير النظام (`ADMIN`)؛ وأي وصول من غيره يرد بـ `403 Forbidden`.
  2. عند إنشاء موظف جديد يتم توليد معرف مشفر `EMP-XXXXXXXX` وتشفير كلمة المرور بـ 12 دورة تمليح، وتخزين دوره (`ADMIN` أو `AGENT` أو `TICKET_ONLY`).
  3. **حظر تخفيض النفس:** لا يمكن للمدير تعديل دوره لتخفيض نفسه من `ADMIN` لتفادي فقدان الإدارة وحظر النظام (`CANNOT_DEMOTE_SELF`).
  4. **حظر إزالة آخر مدير:** يمنع النظام تعطيل أو تخفيض أو حذف حساب المدير النشط الأخير في النظام (`CANNOT_DEMOTE_LAST_ADMIN`).
  5. **حظر حذف النفس:** لا يمكن للمدير حذف حسابه بنفسه (`CANNOT_DELETE_SELF`).
  6. **الحذف الآمن للموظف:** عند حذف حساب موظف (يتطلب تأكيد `confirmEmployeeId`)، يتم حذف الحساب وإلغاء كافة جلساته وتوكناته، بينما تبقى التذاكر والمدفوعات والمعاملات التاريخية سليمة بفضل `onDelete: SetNull` على المفاتيح الخارجية، والاحتفاظ باسم الموظف كنص وصفي (`createdBy`, `addedBy`, `processedBy`).
  7. تعرض صفحة الموظفين تحليلات حية لكل موظف: إجمالي التذاكر المصدرة، المبيعات الإجمالية، المبالغ المحصلة، والاسترجاعات المحسوبة حياً من جدول التذاكر.

---

### 3.11. المصروفات المكتبية والتشغيلية (Office Expenses Management)

- **الوصف:** موديول منفصل ومستقل مخصص لتسجيل ومتابعة نفقات المكتب التشغيلية المصنفة إلى فئتين: الخدمات (`SERVICES`) والتحويلات (`TRANSFERS`).
- **قصة المستخدم (User Story):**
  > "بصفتي وكيل أو مدير، أريد تسجيل المصروفات اليومية للمكتب ومتابعتها، مع عزلها التام عن حسابات أرباح التذاكر الجوية."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. الفئات المقبولة إلزامياً في التحقق هي: `SERVICES` و `TRANSFERS` فقط.
  2. **نطاق الرؤية حسب الدور (Visibility Scope):**
     - مدير النظام يرى جميع المصروفات المسجلة بالمكتب.
     - وكيل الحجز يرى فقط المصروفات التي قام بتسجيلها بنفسه (`where.createdById = currentUser.id`).
     - موظف الإصدار `TICKET_ONLY` محظور تماماً من الوصول للمصروفات (`403 Forbidden`).
  3. تسجيل المصروفات متاح للمدير والوكيل عبر `POST /api/expenses`، بينما الحذف المبدئي (`deletedAt = now()`) مقتصر حصرياً على مدير النظام (`ADMIN`).
  4. **العزل المالي الكامل:** المصروفات المكتبية لا تؤثر إطلاقاً على أسعار بيع أو تكلفة التذاكر أو تقارير أرباح التذاكر الفردية.

---

### 3.12. مؤشرات الأداء والتقارير التنفيذية (Executive KPIs & Reporting)

- **الوصف:** لوحة ذكاء أعمال حية (Business Intelligence) تحسب إجمالي مبيعات الوكالة، التحصيلات، المبالغ المعلقة، وحصص شركات الطيران، وفق سياسة البيانات الحقيقية فقط (Zero-Mock Policy).
- **قصة المستخدم (User Story):**
  > "بصفتي مدير نظام أو وكيل، أريد استعراض تقارير المبيعات ونسب التحصيل الحقيقية دون أي بيانات وهمية، لتقييم الأداء التجاري للوكالة بدقة."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. مؤشرات الأداء (`/api/reports/summary`) تحسب حياً: `totalTickets`, `totalSales`, `totalCollected`, `totalOutstanding`, `totalRefunds`, `totalNetProfit`, `netValue`.
  2. تقرير أداء شركات الطيران (`/api/reports/airlines`) يحسب عدد التذاكر، إجمالي الإيراد، إجمالي المسترد، ونسبة الاسترجاع لكل شركة طيران بدقة.
  3. تقرير دفعات العملاء (`/api/reports/customer-payments`) يعرض جدولاً يوضح لكل عميل وتذكرة: السعر، المدفوع، المتبقي، ونوع الرحلة.
  4. **سياسة البيانات الحقيقية (Zero-Mock):** في حال عدم وجود تذاكر، ترجع التقارير مصفوفات فارغة وأصفاراً حقيقية، ويُمنع نهائياً عرض بيانات تجريبية أو وهمية مسبقة.
  5. **حجب الأرباح:** يتم إخفاء أرباح التكلفة الصافية (`netProfit`) بالكامل عن وكلاء الحجز في كافة شاشات العرض والتقارير.

---

### 3.13. تطبيق الويب التقدمي وتجربة الجوال (PWA & Mobile Experience)

- **الوصف:** منصة مصممة بنمط تطبيق الويب التقدمي (Progressive Web App) قابلة للتثبيت على الأجهزة الذكية وأجهزة سطح المكتب، مع شريط تنقل سفلي للجوال ودعم الوضع الداكن وشاشات اللمس.
- **قصة المستخدم (User Story):**
  > "بصفتي وكيلاً متنقلاً، أريد استخدام المنصة وتثبيتها كتطبيق على جهازي اللوحي أو هاتفي المحمول لإصدار التذاكر ومتابعة الرحلات بسلاسة أثناء التنقل."
- **معايير القبول القابلة للاختبار (Acceptance Criteria):**
  1. يوفر التطبيق ملف `manifest.json` متوافقاً مع معايير PWA (أيقونات 192x192 و 512x512 مع خاصية `maskable`، واسم المنصة، ولون السمة `#131b2e`).
  2. يعمل عامل الخدمة (`sw.js` بنسخة الكاش `africatravel-shell-v2`) على تسريع تحميل الهيكل الثابت للواجهة (HTML, CSS, JS, Fonts).
  3. **عزل الـ API عن الكاش:** يمنع عامل الخدمة منعاً باتاً اعتراض أو تخزين استجابات `/api/*` لضمان أن كافة العمليات المالية وحركات التذاكر تأتي حية ومباشرة من السيرفر.
  4. شريط التنقل السفلي (`bottom-nav.js`) يظهر تلقائياً على الشاشات التي يقل عرضها عن 768px ويتكيف حسب صلاحيات المستخدم (حجب العملاء والمصروفات عن `TICKET_ONLY`، وإخفاء الموظفين عن `AGENT`).
  5. الواجهة تدعم التبديل السلس بين الوضع الداكن ووضع العمليات، وتعتمد نظام متغيرات CSS Tokens لتفادي الألوان الصلبة أو ازدواج عناصر الـ DOM.

---

## 4. قواعد العمل والحالات الحدية الحرجة (Business Rules & Edge Cases)

هذا القسم يوثق القواعد التشغيلية والحدية المؤكدة في كود المشروع الفعلي والتي لا غنى عنها لسلامة الاختبارات:

1. **حجب التكلفة وصافي الأرباح (`costPrice` & `netProfit` Privacy):**
   - حقل `costPrice` وحقل `netProfit` لا يُعرضان إطلاقاً في الواجهة لغير الـ `ADMIN`.
   - دالة `sanitizeTicketForRole` في الـ Controller تقوم بحذف هذين الحقلين من جسم الاستجابة ومن كائن `financials` إذا كان المستخدم ليس `ADMIN`.
   - عند إنشاء تذكرة من قِبل وكيل حجز أو موظف تذاكر، يتم تجاهل قيمة التكلفة وتخزين `0` في قاعدة البيانات.
   - عند تعديل تذكرة، محاولة تعديل `costPrice` من غير الـ ADMIN تلقي خطأ `ForbiddenError` (403).
   - عند استخراج التذكرة بالذكاء الاصطناعي، يتم شطب حقل `costPrice` فوراً من الكائن المستخرج قبل إرساله للعميل.

2. **تسوية محطات الترانزيت في استخراج الذكاء الاصطناعي (Transit Airport Resolution):**
   - عندما تحتوي الرحلة على محطة توقف (Connection/Layover)، مثل رحلة (CAI → IST) ثم (IST → LHR)، فإن مطار `IST` يظهر كوصول للمقطع الأول وإقلاع للمقطع اللاحق، وبالتالي هو مطار ترانزيت قطعي.
   - تنص التعليمات الصارمة في محرك الاستخراج أن الوجهة `destination` يجب أن تكون مطار الوصول للمقطع الأخير فقط (`LHR`). ويُحظر تماماً اعتبار محطة الترانزيت وجهة نهائية.
   - يُستخرج رقم رحلة المقطع الأول فقط (`flightNumber`) الصادر من نقطة الانطلاق، ويُمنع دمج أرقام المقاطع المتعددة بفواصل.

3. **التحقق من رموز المطارات وعدم تفريغ غير المعتمد (Airport Allowlist Validation):**
   - يتم فحص رمزي `origin` و `destination` المستخرجين مقابل قائمة المطارات المعتمدة (`VALID_IATA_AIRPORT_CODES`).
   - في حال كان الرمز صالحاً ككود IATA ثلاثي ولكنه غير مسجل في القائمة الحالية (مثل مطار إقليمي جديد)، **لا يقوم النظام بمسحه أو تفريغه**، بل يرسله كما هو مع إضافته لمصفوفة `unrecognizedFields` لتقوم الواجهة بوضع علامة تنبيه صفراء عليه ليراجعه الموظف يدوياً.

4. **الفرق بين تعديل التذكرة وتعديل الرحلة (Ticket Edit vs Modification Workflow):**
   - **تعديل التذكرة (`PATCH /tickets/:id`):** مخصص لتصحيح أخطاء الإدخال المادية (تصحيح خطأ إملائي في اسم الراكب، رقم التذكرة، رقم الـ PNR، رقم الرحلة، المقعد، الأمتعة، أو تصحيح السعر للمدير). هذا المسار لا يفرض أي رسوم ولا ينشئ سجلاً في جدول التعديلات، بل يحدث حقول التذكرة مباشرة مع تسجيل الفروقات في سجل التدقيق.
   - **تعديل الرحلة (`POST /tickets/:id/modifications`):** مسار عمل تشغيلي رسمي لتغيير مواعيد السفر بطلب المسافر أو شركة الطيران. ينشئ سجلاً منفصلاً `MOD-UUID`، ويحتفظ بلقطة للرحلة السابقة والجديدة، ويسجل رسوم التغيير `changeFee`، ويضيف هذه الرسوم إلى صافي قيمة التذكرة بالدفتر المالي (`netValue`).
   - *واقع الكود الفعلي:* كود النظام اليوم يسمح للوكيل والمدير بتعديل تاريخ الإقلاع عبر `PATCH` دون منع تقني صارم؛ ولكن المسار المالي الصحيح لتغيير المواعيد المرفق برسوم هو عبر `POST .../modifications`.

5. **حظر تخفيض سعر التذكرة دون المبلغ المدفوع (`PRICE_BELOW_PAID` Guard):**
   - عندما يحاول مدير النظام تعديل سعر بيع التذكرة (`ticketPrice`) ليصبح أقل من مجموع المدفوعات المسجلة للتذكرة حتى تاريخه (`totalPaidSoFar`):
   - يرفض الخادم الطلب بحالة `409 Conflict` وبرمز خطأ `PRICE_BELOW_PAID`.
   - لا يقوم الخادم بتصفير الرصيد المتبقي تلقائياً في صمت؛ بل يتطلب من العميل إرسال تأكيد صريح `confirmPriceBelowPaid: true`.
   - في الواجهة الأمامية، يتم اعتراض هذا الرمز وعرض نافذة تأكيد للمستخدم؛ فإذا وافق، يُعاد إرسال الطلب مع تفعيل التأكيد.

6. **مستويات التحقق وحجم مستندات الجوازات (Dual Upload Size Limits):**
   - **الحد المنصي (Vercel Platform Limit):** يفرض وسيط Vercel Serverless Function حداً أقصى صارماً لا يتجاوز **4.5 ميجابايت** على جسم الطلب الكامل (`Request Body Payload`). أي ملف يتجاوز هذا الحد سيرفضه سيرفر Vercel بخطأ `413 Payload Too Large` قبل وصوله لـ Express.
   - **الحد البرمجي (Application Multer Limit):** تم ضبط وسيط `multer` و `EarlyMagicByteStorage` في الكود على حد أقصى قدره **15 ميجابايت**.
   - توثق الوثيقة كلا الرقمين معاً لمنع التضارب: في بيئة النشر السحابي الحالية على Vercel، الحد الفعلي هو 4.5MB؛ بينما يدعم الكود في بيئة الحاويات المستقلة (Docker) حتى 15MB.

7. **فريدة الـ PNR ورقم التذكرة ورقم الجواز (Database Uniqueness & Conflict Handling):**
   - يتم فرض فريدة الـ `pnr` للتذاكر النشطة فقط عبر فهرس جزئي (`Partial Unique Index` حيث `deletedAt IS NULL`).
   - يتم فرض فريدة الـ `ticketNumber` على مستوى جدول التذاكر.
   - يتم فرض فريدة رقم جواز السفر للعملاء النشطين (`passport` في جدول العملاء).
   - عند محاولة إدخال قيمة مكررة، تعترض طبقة الـ Service خطأ بريزما `P2002` وترجع خطأ `409 Conflict` صريحاً بالرموز: `DUPLICATE_PNR`، `DUPLICATE_TICKET_NUMBER`، و `DUPLICATE_PASSPORT` بدلاً من إرجاع خطأ عام 500.

8. **حماية ملفات الجوازات من الضياع والتلف (Passport Storage Cleanup):**
   - عند استبدال ملف جواز سفر لعميل، يتم رفع الملف الجديد أولاً إلى السحابة؛ ثم تُحدث قاعدة البيانات.
   - إذا فشل تحديث قاعدة البيانات، يقوم الخادم بحذف الملف الجديد فوراً كإجراء تعويضي (Compensating Transaction) حتى لا يتبقى ملف يتيم بالسحابة.
   - لا يتم حذف الملف القديم من السحابة إلا بعد نجاح تحديث قاعدة البيانات بالمسار الجديد بالكامل.

9. **واقع كود حذف التذاكر الحالي (Ticket Deletion vs Purge Behavior):**
   - **الحذف عبر `DELETE /api/tickets/:id`:** في كود الخدمة الحالي (`TicketService.deleteTicket`)، يقوم الخادم بحذف التذكرة وسجلاتها المالية نهائياً وحذف متسلسل (`Hard Delete via Cascade`) مع حفظ لقطة مالية كاملة في سجل التدقيق `AuditLog` بالعملية `DELETE_TICKET_WITH_FINANCIALS`، ولا يضع وسم `deletedAt` في قاعدة البيانات.
   - **الحذف النهائي عبر `DELETE /api/tickets/:id/purge`:** يتطلب هذا التابع أن تكون التذكرة محذوفة مبدئياً (`deletedAt != null`) وخالية تماماً من أي سجلات دفع أو استرجاع أو تعديل، مع تأكيد المعرف `confirmTicketId`.
   - *ملاحظة توثيقية:* نظراً لأن نقطة حذف التذاكر العادية تنفذ مسحاً ذرياً مع الأرشفة في الـ AuditLog، فإن التذاكر التي تحتوي على سجلات مالية تُمحى عبر مسار الـ Delete الموثق بالأدمن، بينما مسار الـ Purge مخصص لتطهير التذاكر التي رُصدت كمسودات ملغاة بدون تاريخ مالي.

10. **ضوابط إدارة حسابات الموظفين (Employee Self-Protection & Immunity):**
    - يمنع تماماً أن يقوم المدير بحذف حسابه الشخصي (`CANNOT_DELETE_SELF`).
    - يمنع تماماً أن يقوم المدير بتخفيض دوره إلى رتبة أدنى (`CANNOT_DEMOTE_SELF`).
    - يمنع تماماً تعطيل أو حذف أو تخفيض آخر مدير نظام نشط متبقٍ في الوكالة (`CANNOT_DEMOTE_LAST_ADMIN`).
    - أي تعديل في دور الموظف أو حالته يبطل فوراً كافة جلسات التجديد (`Refresh Tokens`) الخاصة به لفرض إعادة تسجيل الدخول بالصلاحيات الجديدة.

---

## 5. المتطلبات غير الوظيفية (Non-Functional Requirements)

### 5.1. الأمان والرقابة (Security & Auditability)
1. **التحكم في الوصول المعتمد على الأدوار (RBAC Enforcement):** تطبيق صارم في الـ Middleware قبل أي تنفيذ لأي دالة برمجية، مع مراجعة حية لحالة الحساب وقاعدة البيانات في كل طلب (`users.status = ACTIVE`).
2. **تحديد معدل الطلبات الموزع (Distributed Rate Limiting):** استخدام مكتبة `@upstash/ratelimit` مع خادم Redis في بيئة الإنتاج لمزامنة ميزانية الطلبات عبر كافة دوال الخادم بدون خادم (Serverless Instances)، مع دعم نظام الاحتياط في الذاكرة المحلية (In-Memory Sliding Window Fallback) في حال تعذر الاتصال بـ Redis لمنع توقف النظام:
   - مسار الدخول (`/auth/login`): 10 طلبات / 15 دقيقة.
   - مسار تجديد الجلسة (`/auth/refresh`): 30 طلباً / 15 دقيقة.
   - رفع الملفات واستخراج الذكاء الاصطناعي: 10 طلبات / 15 دقيقة مع ميزانية تزامنية (10 رفع، 5 ذكاء اصطناعي).
   - مسارات الـ API العامة: 500 طلب / 15 دقيقة.
3. **أمن الترويسات والـ CSP (Helmet & Content Security Policy):** منع تضمين الموقع في إطارات خارجية (`frame-ancestors 'none'`)، وتطبيق سياسة CSP مقيدة تدعم خطوط Google وخدمات Supabase ومجالات Vercel، ومنع ثغرات تضمين السكربتات المتقاطعة (XSS).
4. **حماية التصفح والملفات الخفية (Path Traversal & Dotfile Protection):** دالة `validatePath` تمنع الوصول لأي ملف أو مجلد يبدأ بنقطة (`.env`, `.git`) وتمنع ثغرات الخروج من مجلد المشروع (`/../`).
5. **سجل التدقيق الشامل (Immutable Audit Logging):** تسجيل كل عملية إنشاء، تعديل، حذف، رفع مستند، استعراض جواز سفر، أو تسجيل دخول في جدول `AuditLog` مع تسجيل اسم الموظف، المعرف، عنوان الـ IP، والـ UserAgent، وتجريد أي كلمات مرور أو توكنات خام من السجلات.

### 5.2. الأداء وقابلية التوسع (Performance & Scalability)
1. **ترقيم القوائم (Pagination):** تطبيق إلزامي للترقيم على استعلامات التذاكر والعملاء والمصروفات وسجلات النشاط بحد افتراضي (25 إلى 50 سجلاً) وحد أقصى 100 سجل في الصفحة.
2. **تحسين قفل قاعدة البيانات:** تنفيذ استعلامات العمليات المالية الحساسة (تسجيل المدفوعات والاسترجاعات) بمستوى عزل متسلسل (`Serializable Isolation`) يضمن عدم تضارب الأرصدة في بيئات العمل المشتركة.
3. **التحميل الاستباقي للجلسة (Proactive Refresh):** واجهة الـ SPA تقوم بفحص وتجديد التوكن استباقياً عند بدء التشغيل لمنع طلبات `401 Unauthorized` المتوازية عند إعادة تحميل الصفحة.

### 5.3. التعريب والتدويل (Localization & Internationalization - i18n)
1. **دعم كامل للغتين العربية والإنجليزية:** تبديل فوري بين العربية (اتجاه من اليمين لليسار `dir="rtl"`) والإنجليزية (اتجاه من اليسار لليمين `dir="ltr"`).
2. **عزل البيانات اللاتينية (LTR Isolation):** نصوص أرقام التذاكر، أرقام الـ PNR، التواريخ، والعملات تلتزم بفئات الـ LTR داخل الواجهة العربية لضمان سلامة العرض ومنع تشوه الأرقام والرموز.
3. **أرقام الجداول الحسابية (`tabular-nums`):** جميع الأرقام والأسعار في الجداول ولوحة المؤشرات تعرض بخاصية المحاذاة الجدولية لسهولة القراءة السريعة والمقارنة.

---

## 6. خارج نطاق العمل والقيود المعروفة (Out of Scope & Known Limitations)

لتوجيه أدوات الاختبار المؤتمتة (مثل TestSprite) نحو الوظائف الحقيقية وعدم توليد اختبارات لميزات غير مطبقة:

1. **العمل بدون إنترنت (No Offline Data Mode):** على الرغم من دعم تثبيت تطبيق الـ PWA، فإن كاش عامل الخدمة مخصص فقط لتحميل واجهة البرنامج (App Shell) بسرعة؛ والتطبيق لا يحتوي على قاعدة بيانات محلية، ولا يدعم إنشاء أو تعديل تذاكر في حال انقطاع الإنترنت.
2. **المصادقة الثنائية (2FA is Placeholder):** علامة التحقق الثنائي في شاشة الإعدادات هي علامة استرشادية غير مفعلة تقنياً في الوقت الحالي (`Not Available / Inactive`).
3. **التحويل الآلي لأسعار الصرف (No Dynamic FX Rates Engine):** يتيح النظام تحديد عملة التذكرة والدفعة (`EGP`, `USD`, `EUR`, `SAR`, `AED`)، ولكنه لا يقوم بتحويل العملات بأسعار صرف حية بين الدفعات؛ حيث تتم المحاسبة المالية لكل تذكرة بعملتها المسجلة.
4. **رحلات المدن المتعددة في الواجهة (Multi-City UI Form):** على الرغم من وجود خيار `Multi City` داخل مخطط Zod، فإن واجهة إنشاء التذاكر الحالية توفر خياري ذهاب فقط (`One Way`) وذهاب وعودة (`Round Trip`).
5. **الربط المباشر مع أنظمة الـ GDS (No Live GDS Ticketing APIs):** لا يتصل النظام مباشرة مع بوابات التوزيع العالمية (Amadeus / Sabre) لإصدار أو إلغاء تذاكر فعلية لدى شركات الطيران، بل يعمل كنظام إدارة وكالة داخلي (Operations & Financial Ledger) يعتمد على الإدخال اليدوي أو قراءة وثائق الحجز بالذكاء الاصطناعي.

---

## 7. ملخص مخطط قاعدة البيانات الفعلي (Active Database Schema Summary)

```prisma
// الأدوار المعتمدة
enum Role {
  ADMIN
  AGENT
  TICKET_ONLY
}

// جدول المستخدمين والموظفين
model User {
  id            String          @id // EMP-XXXXXXXX
  name          String
  email         String          @unique
  role          Role            @default(AGENT)
  title         String
  passwordHash  String
  status        EmployeeStatus  @default(ACTIVE)
  lastActive    DateTime?       @default(now())
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
  tickets       Ticket[]        @relation("TicketCreatedBy")
  payments      Payment[]       @relation("PaymentAddedBy")
  modifications Modification[]  @relation("ModificationProcessedBy")
  refunds       Refund[]        @relation("RefundProcessedBy")
  auditLogs     AuditLog[]      @relation("AuditUser")
  refreshTokens RefreshToken[]
  expenses      Expense[]       @relation("ExpenseCreatedBy")
  @@map("users")
}

// جدول العملاء
model Customer {
  id                    String         @id // CUST-XXXXXXXX
  name                  String
  email                 String?
  phone                 String?
  passport              String?        @unique
  passportDocPath       String?        // customers/{id}/passport-{uuid}.jpg
  passportDocUploadedAt DateTime?
  nationality           String         @default("Egyptian (EGY)")
  isVip                 Boolean        @default(false)
  memberSince           String         @default("2024")
  deletedAt             DateTime?
  createdAt             DateTime       @default(now())
  updatedAt             DateTime       @updatedAt
  tickets               Ticket[]
  notes                 CustomerNote[]
  @@index([deletedAt])
  @@map("customers")
}

// جدول التذاكر الجوية
model Ticket {
  id                     String         @id // TK-XXXXXXXX
  ticketNumber           String?        @unique
  pnr                    String?        // فريد للتذاكر النشطة فقط
  customerId             String
  customer               Customer       @relation(fields: [customerId], references: [id])
  passengerName          String
  phone                  String?
  passport               String?
  nationality            String?
  dob                    String?
  email                  String?
  airline                String
  airlineCode            String
  flightNumber           String
  returnFlightNumber     String?
  origin                 String
  originTerminal         String?
  originAirportName      String?
  destination            String
  destinationTerminal    String?
  destinationAirportName String?
  departureDate          DateTime?
  arrivalDate            DateTime?
  returnDepartureDate    DateTime?
  returnArrivalDate      DateTime?
  tripType               String         @default("One Way") // One Way | Round Trip
  flightDuration         String?
  cabinClass             String         @default("Economy (Y)")
  seat                   String?
  baggage                String?
  ticketPrice            Decimal        @db.Decimal(12, 2)
  costPrice              Decimal?       @db.Decimal(12, 2)
  currency               String         @default("EGP")
  status                 String         @default("CONFIRMED")
  createdBy              String
  createdById            String?
  user                   User?          @relation("TicketCreatedBy", fields: [createdById], references: [id])
  deletedAt              DateTime?
  createdAt              DateTime       @default(now())
  updatedAt              DateTime       @updatedAt
  payments               Payment[]
  modifications          Modification[]
  refunds                Refund[]
  @@index([createdById])
  @@index([customerId])
  @@index([deletedAt])
  @@map("tickets")
}

// جدول المدفوعات
model Payment {
  id        String   @id // PAY-XXXXXXXX
  ticketId  String
  ticket    Ticket   @relation(fields: [ticketId], references: [id], onDelete: Cascade)
  amount    Decimal  @db.Decimal(12, 2)
  currency  String   @default("EGP")
  method    String   @default("Credit Card")
  reference String?
  date      DateTime @default(now())
  addedBy   String
  addedById String?
  notes     String?
  createdAt DateTime @default(now())
  @@map("payments")
}

// جدول تعديل الرحلات
model Modification {
  id             String   @id // MOD-XXXXXXXX
  ticketId       String
  ticket         Ticket   @relation(fields: [ticketId], references: [id], onDelete: Cascade)
  title          String
  originalFlight Json
  newFlight      Json
  changeFee      Decimal  @default(0) @db.Decimal(12, 2)
  currency       String   @default("EGP")
  reason         String
  requestedBy    String
  processedBy    String
  processedById  String?
  date           DateTime @default(now())
  status         String   @default("COMPLETED")
  createdAt      DateTime @default(now())
  @@map("modifications")
}

// جدول الاسترجاعات
model Refund {
  id             String    @id // RF-XXXXXXXX
  ticketId       String
  ticket         Ticket    @relation(fields: [ticketId], references: [id], onDelete: Cascade)
  originalAmount Decimal?  @db.Decimal(12, 2)
  totalPaid      Decimal?  @db.Decimal(12, 2)
  amount         Decimal   @db.Decimal(12, 2)
  currency       String    @default("EGP")
  reason         String
  status         String    @default("COMPLETED")
  requestedDate  DateTime  @default(now())
  processedDate  DateTime? @default(now())
  processedBy    String
  processedById  String?
  createdAt      DateTime  @default(now())
  @@map("refunds")
}

// جدول توكنات التجديد
model RefreshToken {
  id         String   @id @default(cuid())
  tokenHash  String   @unique
  userId     String
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  revoked    Boolean  @default(false)
  rememberMe Boolean  @default(true)
  expiresAt  DateTime
  createdAt  DateTime @default(now())
  @@index([userId])
  @@map("refresh_tokens")
}

// جدول المصروفات التشغيلية
model Expense {
  id          String          @id @default(cuid())
  category    ExpenseCategory // SERVICES | TRANSFERS
  amount      Decimal         @db.Decimal(12, 2)
  currency    String          @default("EGP")
  description String
  date        DateTime        @default(now())
  createdBy   String
  createdById String?
  deletedAt   DateTime?
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt
  @@index([createdById])
  @@index([date])
  @@map("expenses")
}

// سجل التدقيق الموحد
model AuditLog {
  id          String   @id @default(cuid())
  timestamp   DateTime @default(now())
  user        String
  userId      String?
  action      String
  ticketId    String?
  customerId  String?
  description String
  metadata    Json?
  ip          String?
  userAgent   String?
  createdAt   DateTime @default(now())
  @@map("audit_logs")
}
```
